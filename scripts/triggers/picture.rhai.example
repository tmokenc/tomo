fn meta() {
    // `match` is a reserved keyword in Rhai, so the key must be quoted.
    #{
        name: "picture",
        "match": #{ has_image: true },
    }
}

fn execute(ctx) {
    ctx.react("📸");
}
